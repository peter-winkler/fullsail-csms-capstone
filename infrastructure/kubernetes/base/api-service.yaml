apiVersion: v1
kind: Service
metadata:
  name: kinatrax-api
  labels:
    app.kubernetes.io/name: kinatrax-api
    app.kubernetes.io/component: api
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: kinatrax-api
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
